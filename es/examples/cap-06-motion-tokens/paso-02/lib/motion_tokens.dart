// Generado por build.mjs. No editar a mano.
// Este archivo es la traduccion Dart de tokens/primitive.json.

import 'package:flutter/animation.dart';

class MotionTokens {
  MotionTokens._();

  // Duraciones
  static const Duration durationInstant = Duration(milliseconds: 0);
  static const Duration durationFast = Duration(milliseconds: 150);
  static const Duration durationNormal = Duration(milliseconds: 250);
  static const Duration durationSlow = Duration(milliseconds: 400);
  static const Duration durationSlower = Duration(milliseconds: 600);

  // Easing curves
  static const Cubic easeOut = Cubic(0.16, 1, 0.3, 1);
  static const Cubic easeIn = Cubic(0.7, 0, 0.84, 0);
  static const Cubic easeInOut = Cubic(0.65, 0, 0.35, 1);
  static const Cubic easeEmphasized = Cubic(0.2, 0, 0, 1);
}
